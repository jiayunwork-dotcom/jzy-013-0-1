PORT=3000
NODE_ENV=production

# postgres (used by `docker compose up` and normal production run)
DB_TYPE=postgres
DB_HOST=db
DB_PORT=5432
DB_USER=buck
DB_PASSWORD=buck_secret
DB_NAME=buckcalc
DB_SYNCHRONIZE=true

# tolerances (override if needed)
VOLT_SECOND_TOL=1e-9
BOUNDARY_TOL=1e-12
